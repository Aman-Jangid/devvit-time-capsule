// edit one of the images from assets/teaser folder and return to the caller to include it in the teaser post

const getPostDetails = (details: {}) => {
  // const image = import(`../public/assets/teaser/${details?.theme || 'default'}.jpg`);
  // editImage();
};

const editImage = (image: string) => {};

export default getPostDetails;
